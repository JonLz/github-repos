# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'
use_frameworks!

target 'github-repos' do
  pod 'Anchorage', '~> 4.3'
  pod 'PromiseKit', '~> 6.10.0'
  pod 'SwiftChart', '~> 1.0.1'
end
